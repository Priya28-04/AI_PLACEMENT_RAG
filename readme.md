1.pip install uv

2.first create environment
uv venv
activate it

3.then run requirements
uv pip install -r requirements.txt

4.then run 
uv run create_vectorstore.py

5.then streamlit
streamlit run app.py